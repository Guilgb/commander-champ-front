export interface UserList {
  id: number;
  name: string;
  email: string;
  role: string;
  created_at: string;
}

export type User = {
  id: number
  name: string
  email: string
  role: string
  created_at: string
}